import { SafeAreaView, Text, View } from 'react-native';
import React from 'react'

const HotelsScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text className="text-red-700">ITS HOTELS!</Text>
    </View>
  )
}

export default HotelsScreen